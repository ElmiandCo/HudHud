export default async function handler(req,res){if(req.method!=="POST"){res.setHeader("Allow","POST");return res.status(405).json({error:"Method not allowed"})}const controlUrl=process.env.HUDHUD_CONTROL_URL,token=process.env.HUDHUD_BRAIN_TOKEN;if(!controlUrl||!token)return res.status(503).json({error:"HudHud local control is not configured."});const action=typeof req.body?.action==="string"?req.body.action:"";const allowed=["health","start-gpt4all","start-tailscale","start-funnel","restart-bridge","start-everything"];if(!allowed.includes(action))return res.status(400).json({error:"Unknown system action."});try{const r=await fetch(controlUrl,{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${token}`},body:JSON.stringify({action})});const raw=await r.text();let data={};try{data=JSON.parse(raw)}catch{data={message:raw}}if(!r.ok)return res.status(r.status).json(data);return res.status(200).json(data)}catch(error){return res.status(502).json({error:"HudHud local control agent is unreachable."})}}